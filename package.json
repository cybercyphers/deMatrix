{
  "name": "dematrix",
  "version": "1.1.3",
  "description": "Api for developers. free and many more",
  "main": "./dist/index.cjs",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "scripts": {
    "build": "if not exist dist mkdir dist && esbuild index.js --bundle --minify --platform=node --outfile=dist/index.js --format=esm && esbuild index.js --bundle --minify --platform=node --outfile=dist/index.cjs --format=cjs",
    "test": "echo \"No tests\" && exit 0", 
    "start": "npm run build && node index.js"
  },
  "keywords": [
    "matrix",
    "decentralized"
  ],
  "author": "cyber cyphers",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cybercyphers/deMatrix.git"
  },
  "dependencies": {
    "axios": "^1.18.1",
    "qrcode": "^1.5.3"
  },
  "devDependencies": {
    "esbuild": "latest"
  },
  "engines": {
    "node": ">=20"
  },
  "files": [
    "dist"
  ]
}
